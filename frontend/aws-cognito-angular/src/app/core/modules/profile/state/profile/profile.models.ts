export interface UpdateUserProfileInput {
  name: string;
  company: string;
  role: string;
}

export interface UserProfile {
  phoneNumber: string;
  name: string;
  company: string;
  role: string;
}
